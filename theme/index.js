/**
 * theme/index.js — Barrel export
 * Import from 'theme/index.js' (or '@/theme' with a path alias) to
 * access all branding tokens from a single import statement.
 *
 * Example:
 *   import { theme } from './theme/index.js';
 *   console.log(theme.firm.name);       // "Pinnacle Law"
 *   console.log(theme.colors.accent);   // "#8B1538"
 */

export { theme, default } from './config.js';
